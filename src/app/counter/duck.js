//Actions

//Initial State
const InitialState = {
  counter: 0
};

//Reducer
export default function reducer(state = InitialState, action = {}) {
  switch(action.type) {
    default: return state;
  }
}

